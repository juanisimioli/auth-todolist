/**
 * Name of the cookie key used for storing JWT tokens.
 * @constant {string}
 */
export const AUTH_TOKEN = "auth-token";

/**
 * Number of seconds in 30 days.
 * Used for setting the max age of JWT tokens and cookies.
 * @constant {number}
 */
export const DAYS_30 = 60 * 60 * 24 * 30;
